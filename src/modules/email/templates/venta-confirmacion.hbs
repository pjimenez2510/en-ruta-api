<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirmación de Compra - {{nombreCooperativa}}</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
            color: #333;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px 20px;
            text-align: center;
        }
        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: bold;
        }
        .header p {
            margin: 10px 0 0 0;
            font-size: 16px;
            opacity: 0.9;
        }
        .content {
            padding: 30px 20px;
        }
        .success-message {
            background-color: #d4edda;
            border: 1px solid #c3e6cb;
            color: #155724;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 25px;
            text-align: center;
            font-weight: bold;
        }
        .info-section {
            margin-bottom: 25px;
            padding: 20px;
            background-color: #f8f9fa;
            border-radius: 8px;
            border-left: 4px solid #667eea;
        }
        .info-section h3 {
            margin: 0 0 15px 0;
            color: #667eea;
            font-size: 18px;
        }
        .info-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
            padding: 5px 0;
        }
        .info-row:last-child {
            margin-bottom: 0;
        }
        .info-label {
            font-weight: bold;
            color: #666;
        }
        .info-value {
            color: #333;
            text-align: right;
        }
        .boletos-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 15px;
        }
        .boletos-table th,
        .boletos-table td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        .boletos-table th {
            background-color: #667eea;
            color: white;
            font-weight: bold;
        }
        .boletos-table tr:nth-child(even) {
            background-color: #f9f9f9;
        }
        .codigo-acceso {
            background-color: #667eea;
            color: white;
            padding: 4px 8px;
            border-radius: 4px;
            font-family: monospace;
            font-weight: bold;
        }
        .total-section {
            background-color: #e7f3ff;
            border: 2px solid #667eea;
            border-radius: 8px;
            padding: 20px;
            margin: 25px 0;
        }
        .total-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 10px;
            font-size: 16px;
        }
        .total-final {
            border-top: 2px solid #667eea;
            padding-top: 10px;
            margin-top: 10px;
            font-weight: bold;
            font-size: 18px;
            color: #667eea;
        }
        .footer {
            background-color: #2c3e50;
            color: white;
            padding: 20px;
            text-align: center;
            font-size: 14px;
        }
        .footer p {
            margin: 5px 0;
        }
        .note {
            background-color: #fff3cd;
            border: 1px solid #ffeaa7;
            color: #856404;
            padding: 15px;
            border-radius: 8px;
            margin-top: 20px;
        }
        @media (max-width: 600px) {
            .container {
                margin: 0;
                border-radius: 0;
            }
            .info-row {
                flex-direction: column;
            }
            .info-value {
                text-align: left;
                margin-top: 5px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>¡Compra Confirmada!</h1>
            <p>{{nombreCooperativa}}</p>
        </div>
        
        <div class="content">
            <div class="success-message">
                🎉 ¡Hola {{nombreCliente}}! Tu compra ha sido procesada exitosamente.
            </div>
            
            <div class="info-section">
                <h3>📋 Detalles de la Compra</h3>
                <div class="info-row">
                    <span class="info-label">Número de Venta:</span>
                    <span class="info-value">#{{numeroVenta}}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Fecha de Compra:</span>
                    <span class="info-value">{{fechaVenta}}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Estado del Pago:</span>
                    <span class="info-value">{{estadoPago}}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Método de Pago:</span>
                    <span class="info-value">{{metodoPago}}</span>
                </div>
            </div>
            
            <div class="info-section">
                <h3>🚌 Información del Viaje</h3>
                <div class="info-row">
                    <span class="info-label">Ruta:</span>
                    <span class="info-value">{{nombreRuta}}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Fecha del Viaje:</span>
                    <span class="info-value">{{fechaViaje}}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Hora de Salida:</span>
                    <span class="info-value">{{horaSalida}}</span>
                </div>
                <div class="info-row">
                    <span class="info-label">Autobús:</span>
                    <span class="info-value">{{numeroAutobus}} - {{placaAutobus}}</span>
                </div>
            </div>
            
            <div class="info-section">
                <h3>🎫 Tus Boletos</h3>
                <table class="boletos-table">
                    <thead>
                        <tr>
                            <th>Código</th>
                            <th>Pasajero</th>
                            <th>Asiento</th>
                            <th>Ruta</th>
                            <th>Precio</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each boletos}}
                        <tr>
                            <td><span class="codigo-acceso">{{codigoAcceso}}</span></td>
                            <td>{{nombrePasajero}}</td>
                            <td>{{numeroAsiento}}</td>
                            <td>{{ciudadOrigen}} → {{ciudadDestino}}</td>
                            <td>${{precio}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
            
            <div class="total-section">
                <div class="total-row">
                    <span>Subtotal:</span>
                    <span>${{totalSinDescuento}}</span>
                </div>
                {{#if totalDescuentos}}
                <div class="total-row">
                    <span>Descuentos:</span>
                    <span>-${{totalDescuentos}}</span>
                </div>
                {{/if}}
                <div class="total-row total-final">
                    <span>Total Final:</span>
                    <span>${{totalVenta}}</span>
                </div>
            </div>
            
            <div class="note">
                <strong>📌 Importante:</strong>
                <ul>
                    <li>Guarda este email como comprobante de tu compra</li>
                    <li>Presenta tu código de acceso al momento de abordar</li>
                    <li>Llega al terminal 30 minutos antes de la hora de salida</li>
                    <li>Porta tu documento de identidad</li>
                </ul>
            </div>
        </div>
        
        <div class="footer">
            <p><strong>{{nombreCooperativa}}</strong></p>
            <p>Gracias por viajar con nosotros</p>
            <p>Para consultas, contacta nuestro servicio al cliente</p>
        </div>
    </div>
</body>
</html> 